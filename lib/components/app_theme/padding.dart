import 'package:flutter/material.dart';

const myContentPadding = EdgeInsets.all(10);

const myDisplayContentPadding = EdgeInsets.all(15);

const menuItemPadding = EdgeInsets.only(top: 10, left: 20, right: 20);

const formWidgetPadding = EdgeInsets.only(top: 4, left: 10, right: 10);

const formPadding = EdgeInsets.only(left: 20, right: 20);

const space = EdgeInsets.only(top: 20);

const myCardPadding = EdgeInsets.only(left: 2, right: 2);
