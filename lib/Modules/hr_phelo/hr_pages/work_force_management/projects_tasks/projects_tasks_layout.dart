import 'package:flutter/material.dart';

class ProjectsTasksLayout extends StatelessWidget {
  const ProjectsTasksLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My projects and tasks management'),);
  }
}