import 'package:flutter/material.dart';

class MyTimePlanner extends StatelessWidget {
  const MyTimePlanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My time management'),);
  }
}