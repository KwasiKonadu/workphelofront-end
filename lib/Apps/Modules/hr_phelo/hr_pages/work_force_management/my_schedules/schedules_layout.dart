import 'package:flutter/material.dart';

class SchedulesLayout extends StatelessWidget {
  const SchedulesLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My schedule page'),);
  }
}